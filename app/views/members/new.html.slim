= provide(:title, "Add Member")

h1 = 

.row
  .six.columns
    h1 = yield(:title)

    = form_for(@member) do |f|
      = f.label :name
      = f.text_field :name
      br
      = f.label :email
      = f.text_field :email
      = f.submit